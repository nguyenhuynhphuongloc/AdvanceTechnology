# Phase 2A: Test Checklist

> **Ngày**: 16 May 2026
> **Phase**: 2A — Product & Catalog Marketplace Foundation

---

## Setup

1. Start all services (docker-compose up)
2. Register admin account
3. Register seller account
4. Admin login → get admin JWT
5. Seller login → get seller JWT
6. Seller tạo shop: `POST /api/v1/seller/shop`
7. Admin approve shop: `PATCH /api/v1/admin/shops/:shopId/approve`

---

## Seller Product Flow

- [ ] **S-01**: Seller tạo product → Product có `approvalStatus = 'pending'`, `shopId` đúng
- [ ] **S-02**: Seller GET `/api/v1/seller/products` → Thấy product mới tạo
- [ ] **S-03**: Seller GET `/api/v1/seller/products/:id` → Xem chi tiết product
- [ ] **S-04**: Seller PATCH product → Product cập nhật, nếu bị rejected thì tự chuyển về pending
- [ ] **S-05**: Seller DELETE product → Product bị xóa
- [ ] **S-06**: Seller chưa có shop tạo product → 404 `"You do not have a shop"`
- [ ] **S-07**: Seller submit draft product → approvalStatus = pending
- [ ] **S-08**: Seller submit rejected product → approvalStatus = pending
- [ ] **S-09**: Seller submit approved product → 400 error

---

## Admin Approval Flow

- [ ] **A-01**: Admin GET `/api/v1/admin/products` → Thấy product pending
- [ ] **A-02**: Admin approve product → approvalStatus = 'approved', approvedAt set
- [ ] **A-03**: Admin reject product → approvalStatus = 'rejected', rejectionReason set
- [ ] **A-04**: Admin hide product → approvalStatus = 'hidden', isActive = false
- [ ] **A-05**: Admin create product with shopId → approvalStatus = 'approved' (auto-approved)
- [ ] **A-06**: Admin create product without shopId → 400 error
- [ ] **A-07**: Admin approve already-approved product → 400 error
- [ ] **A-08**: Admin assign legacy product to shop → shopId/sellerId set
- [ ] **A-09**: Admin filter products by `approvalStatus=pending` → Chỉ thấy pending
- [ ] **A-10**: Admin filter products by `shopId=X` → Chỉ thấy products của shop đó

---

## Public Product Visibility

- [ ] **P-01**: Public GET `/api/v1/products` → Thấy approved products, KHÔNG thấy pending
- [ ] **P-02**: Public GET `/api/v1/products/:slug` (pending product) → 404
- [ ] **P-03**: Public GET `/api/v1/products/:slug` (approved product) → 200, đầy đủ data
- [ ] **P-04**: GET `/api/v1/shops/:slug/products` → Thấy approved products của shop
- [ ] **P-05**: GET `/api/v1/shops/:slug/products` (shop chưa approved) → 404
- [ ] **P-06**: GET `/api/v1/shops/:slug/products` (shop không có product) → Empty list
- [ ] **P-07**: GET `/api/v1/products/:slug/related` → Chỉ related products approved

---

## Security Tests

- [ ] **SEC-01**: Unauthenticated call `/api/v1/seller/products` → 401
- [ ] **SEC-02**: Customer role call `/api/v1/seller/products` → 403
- [ ] **SEC-03**: Seller call `/api/v1/admin/products/moderation/:id/approve` → 403
- [ ] **SEC-04**: Seller sửa product của seller khác → 404 (không có quyền)
- [ ] **SEC-05**: Seller truyền shopId trong body khi tạo product → Không bị override (server tự lấy shopId)
- [ ] **SEC-06**: Admin gọi `/api/v1/admin/products/moderation` với JWT không phải admin → 403

---

## Backward Compatibility

- [ ] **BC-01**: `GET /api/v1/products` → Vẫn chạy bình thường
- [ ] **BC-02**: `GET /api/v1/categories` → Vẫn chạy bình thường
- [ ] **BC-03**: Admin product CRUD cũ → Vẫn hoạt động
- [ ] **BC-04**: Media upload APIs → Vẫn hoạt động
- [ ] **BC-05**: Legacy products (không shopId) vẫn hiển thị ở `/products`
- [ ] **BC-06**: Response ProductCardDto có thêm `shopId`, `sellerId`, `approvalStatus` → Frontend cần xử lý field mới

---

## Shop Validation

- [ ] **SHOP-01**: Seller với shop rejected tạo product → 403
- [ ] **SHOP-02**: Seller với shop suspended tạo product → 403
- [ ] **SHOP-03**: Seller với shop pending tạo product → 200, product ở trạng thái pending

---

## End-to-End Flow

- [ ] **E2E-01**: Seller tạo product → Admin duyệt → Public thấy product
- [ ] **E2E-02**: Seller tạo product → Admin từ chối → Seller sửa → Re-submit → Admin duyệt → Public thấy
- [ ] **E2E-03**: Admin hide product → Public không thấy product đó nữa

---

## Build Verification

- [ ] product-service: `npm run build` → ✅ Pass
- [ ] store-service: `npm run build` → ✅ Pass
- [ ] api-gateway: `npm run build` → ✅ Pass

---

*Document generated by Senior Backend Architect / Full-stack Engineer Agent*
*Last updated: 16 May 2026*
