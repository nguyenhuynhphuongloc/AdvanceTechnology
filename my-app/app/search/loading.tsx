import { StorefrontLoadingPage } from "../../components/storefront/StorefrontLoadingPage";

export default function LoadingSearchPage() {
  return (
    <StorefrontLoadingPage
      title="Loading search results"
      description="Searching the live catalog and assembling the current storefront results."
    />
  );
}
